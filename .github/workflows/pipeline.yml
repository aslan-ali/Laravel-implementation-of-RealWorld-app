name: My-GithubActions
env:
  APPLICATION_NAME: "TEST"
  PACKAGE_NAME: "TEST-DEPLOY-VER-${{ github.sha }}"

on:
  push:
    branches: [ "main" ]          
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  my_building:
    runs-on: ubuntu-latest
    
    steps:
    - name: Hello World
      run: echo "Hello World from Build"
    - name: Run few commands
      run: |
        echo "Hello Message1"
        echo "Hello Message2"
        echo "Application Name: ${{ env.APPLICATION_NAME }}"
    
    - name: Git Clone my repo to container
      uses: actions/checkout@v1
    - name: ensure our repo here
      run: ls -ltr
    
    
    
  my_deploy: 
    runs-on: ubuntu-latest
    needs: [my_building]
    env:
      VARIABLE1: "HI PEOPLE"
      VARIABLE2: "HI WORLD"
      
    steps:
    - name: Hello World
      run: echo "Hello World From Deploy"
    - name: DEPLOYING_PACKAGE
      run: |
        echo  " Deploy package by Aslan"
        echo "Deploy package name ${{ env.PACKAGE_NAME }}"
    - name: PRINT VARIABLES TEST FOR THIS JOB 
      run: |
        echo "I wanna say {{ env.VARIABLE1 }}"
        echo "I wanna cry {{ env.VARIABLE2 }}"
        echo "I wanna say this LOCAL variable only work in this -name print variables like that {{ env.LOCAL_VARIABLE }}"
      env:
        LOCAL_VARIABLE: "HI_PEOPLE_THIS IS LOCAL VARIABLE"
